{
	"name": "mhaol-monorepo",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "pnpm run app:node & wait-on tcp:1530 && pnpm run app:frontend & pnpm run app:player & wait",
		"dev:node": "set -a; . ./.env 2>/dev/null; set +a; PORT=1530 cargo run -p mhaol-node --bin mhaol-node",
		"dev:frontend": "pnpm --filter frontend dev",
		"dev:player": "pnpm --filter player dev",
		"build": "pnpm --filter frontend build",
		"build:node": "cargo build --release --bin mhaol-node",
		"preview": "pnpm --filter frontend preview",
		"test": "pnpm -r run test && cargo test -p mhaol-node",
		"lint": "pnpm -r run lint",
		"format": "pnpm -r run format",
		"check": "pnpm -r run check && cargo check -p mhaol-node",
		"clean": "pnpm -r run clean && cargo clean && rm -f mhaol.db mhaol.db-wal mhaol.db-shm apps/node/mhaol.db apps/node/mhaol.db-wal apps/node/mhaol.db-shm",
		"reset": "rm -rf \"${DATA_DIR:-$HOME/Documents/mhaol}\"",
		"signaling:dev": "dotenv -- pnpm --filter signaling dev",
		"signaling:deploy": "dotenv -- pnpm --filter signaling run deploy",
		"signaling:serve": "cargo run -p mhaol-signaling -- serve",
		"signaling:setup": "cargo run -p mhaol-signaling -- setup",
		"build:signaling": "cargo build --release --bin mhaol-signaling",
		"app:node": "set -a; . ./.env 2>/dev/null; set +a; PORT=1530 cargo run -p mhaol-node --bin mhaol-node",
		"app:frontend": "pnpm --filter frontend dev",
		"app:player": "pnpm --filter player dev",
		"app:shepperd": "pnpm --filter shepperd dev",
		"app:shepperd:build": "pnpm --filter shepperd build",
		"android:dev": "adb reverse tcp:1530 tcp:1530; cd apps/frontend && cargo tauri android dev",
		"android:build": "cd apps/frontend && cargo tauri android build",
		"android:build:apk": "cd apps/frontend && cargo tauri android build --apk",
		"install:deps": "sudo apt-get update && sudo apt-get install -y build-essential pkg-config libssl-dev libonig-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev libgstreamer-plugins-bad1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav"
	},
	"engines": {
		"node": ">=18",
		"pnpm": ">=9"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"better-sqlite3",
			"esbuild"
		]
	},
	"devDependencies": {
		"dotenv-cli": "^11.0.0",
		"wait-on": "^9.0.4"
	}
}
