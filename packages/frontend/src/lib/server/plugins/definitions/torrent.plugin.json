{
	"name": "torrent",
	"version": "1.0.0",
	"description": "BitTorrent client for downloading and seeding torrents",
	"compatibility": {
		"mobile": false,
		"computer": true
	},
	"processes": [
		{
			"id": "torrent-server",
			"binary": "./node_modules/.bin/tsx",
			"args": ["../torrent/src/server/server.ts"],
			"port": 3050,
			"portEnv": "TORRENT_PORT",
			"env": {
				"DB_PATH": "",
				"TORRENT_DOWNLOAD_DIR": {
					"type": "libraryPath",
					"metaKey": "torrent.libraryId",
					"fallback": "~/Downloads/torrents"
				}
			},
			"logPrefix": "torrent",
			"healthCheck": {
				"path": "/health",
				"retries": 20,
				"intervalMs": 500
			}
		}
	],
	"schema": {
		"sql": "CREATE TABLE IF NOT EXISTS torrent_downloads (\n    info_hash TEXT PRIMARY KEY,\n    name TEXT NOT NULL,\n    size INTEGER NOT NULL DEFAULT 0,\n    progress REAL NOT NULL DEFAULT 0,\n    state TEXT NOT NULL DEFAULT 'initializing',\n    download_speed INTEGER NOT NULL DEFAULT 0,\n    upload_speed INTEGER NOT NULL DEFAULT 0,\n    peers INTEGER NOT NULL DEFAULT 0,\n    seeds INTEGER NOT NULL DEFAULT 0,\n    added_at INTEGER NOT NULL,\n    eta INTEGER,\n    output_path TEXT,\n    source TEXT NOT NULL,\n    created_at TEXT NOT NULL DEFAULT (datetime('now')),\n    updated_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TRIGGER IF NOT EXISTS torrent_downloads_updated_at\n    AFTER UPDATE ON torrent_downloads\n    FOR EACH ROW\nBEGIN\n    UPDATE torrent_downloads SET updated_at = datetime('now') WHERE info_hash = OLD.info_hash;\nEND;"
	},
	"locals": [
		{ "key": "torrentBaseUrl", "source": "processUrl", "processId": "torrent-server" },
		{ "key": "torrentAvailable", "source": "processAvailable", "processId": "torrent-server" }
	]
}
