{
	"name": "yt-download",
	"version": "1.0.0",
	"description": "YouTube video/audio downloader via Rust yt-dlp server",
	"compatibility": {
		"mobile": true,
		"computer": true
	},
	"processes": [
		{
			"id": "ytdl-server",
			"binary": "../rust-yt-dlp/target/debug/mhaol-yt-dlp-server",
			"binaryEnv": "YTDL_BIN",
			"port": 3040,
			"portEnv": "YTDL_PORT",
			"env": {
				"YTDL_OUTPUT_DIR": {
					"type": "libraryPath",
					"metaKey": "youtube.libraryId",
					"fallback": "~/Downloads/youtube"
				},
				"YTDL_CORS_ORIGIN": "http://localhost:1530",
				"RUST_LOG": "info",
				"YTDL_PO_TOKEN": {
					"type": "settingValue",
					"key": "youtube.poToken",
					"optional": true
				},
				"YTDL_COOKIES": {
					"type": "settingValue",
					"key": "youtube.cookies",
					"optional": true
				}
			},
			"logPrefix": "ytdl-rust",
			"healthCheck": {
				"path": "/api/status",
				"retries": 20,
				"intervalMs": 500
			}
		}
	],
	"schema": {
		"sql": "CREATE TABLE IF NOT EXISTS youtube_downloads (\n    download_id TEXT PRIMARY KEY,\n    url TEXT NOT NULL,\n    video_id TEXT NOT NULL,\n    title TEXT NOT NULL,\n    state TEXT NOT NULL DEFAULT 'pending',\n    progress REAL NOT NULL DEFAULT 0,\n    downloaded_bytes INTEGER NOT NULL DEFAULT 0,\n    total_bytes INTEGER NOT NULL DEFAULT 0,\n    output_path TEXT,\n    error TEXT,\n    mode TEXT NOT NULL DEFAULT 'audio',\n    quality TEXT NOT NULL DEFAULT 'high',\n    format TEXT NOT NULL DEFAULT 'aac',\n    video_quality TEXT,\n    video_format TEXT,\n    thumbnail_url TEXT,\n    duration_seconds REAL,\n    created_at TEXT NOT NULL DEFAULT (datetime('now')),\n    updated_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TRIGGER IF NOT EXISTS youtube_downloads_updated_at\n    AFTER UPDATE ON youtube_downloads\n    FOR EACH ROW\nBEGIN\n    UPDATE youtube_downloads SET updated_at = datetime('now') WHERE download_id = OLD.download_id;\nEND;"
	},
	"settings": [
		{ "key": "youtube.downloadMode", "default": "audio" },
		{ "key": "youtube.defaultQuality", "default": "high" },
		{ "key": "youtube.defaultFormat", "default": "aac" },
		{ "key": "youtube.defaultVideoQuality", "default": "best" },
		{ "key": "youtube.defaultVideoFormat", "default": "mp4" },
		{ "key": "youtube.poToken", "default": "" },
		{ "key": "youtube.cookies", "default": "" }
	],
	"locals": [
		{ "key": "ytdlBaseUrl", "source": "processUrl", "processId": "ytdl-server" },
		{ "key": "ytdlAvailable", "source": "processAvailable", "processId": "ytdl-server" }
	]
}
