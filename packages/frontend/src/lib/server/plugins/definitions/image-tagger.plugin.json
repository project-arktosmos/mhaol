{
	"name": "image-tagger",
	"version": "1.0.0",
	"description": "ML-powered image tagging using SigLIP zero-shot classification",
	"compatibility": {
		"mobile": false,
		"computer": true
	},
	"processes": [
		{
			"id": "image-tagger-server",
			"binary": "./node_modules/.bin/tsx",
			"args": ["../image-tagger/src/server/server.ts"],
			"port": 3060,
			"portEnv": "IMAGE_TAGGER_PORT",
			"env": {
				"DB_PATH": ""
			},
			"logPrefix": "image-tagger",
			"healthCheck": {
				"path": "/health",
				"retries": 30,
				"intervalMs": 500
			}
		}
	],
	"schema": {
		"sql": "CREATE TABLE IF NOT EXISTS image_tags (\n    id TEXT PRIMARY KEY,\n    library_item_id TEXT NOT NULL REFERENCES library_items(id) ON DELETE CASCADE,\n    tag TEXT NOT NULL,\n    score REAL NOT NULL,\n    created_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE INDEX IF NOT EXISTS idx_image_tags_library_item_id ON image_tags(library_item_id);\nCREATE INDEX IF NOT EXISTS idx_image_tags_tag ON image_tags(tag);"
	},
	"locals": [
		{ "key": "imageTaggerBaseUrl", "source": "processUrl", "processId": "image-tagger-server" },
		{ "key": "imageTaggerAvailable", "source": "processAvailable", "processId": "image-tagger-server" }
	]
}
